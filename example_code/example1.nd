# Very first example file of the nD language
# I will finish this project no matter what lets get to it!!!



fun main () : int
{
    let x : int = 1;
    let f : int -> int = (fn x => x - 1);
    return f x;
}
